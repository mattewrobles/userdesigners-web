---
interface Props {
  hover?: boolean;
  as?: "div" | "article" | "li";
  "data-gsap"?: string;
}

const { hover = true, as = "div", "data-gsap": dataGsap } = Astro.props;
const Tag = as;
const attrs = dataGsap ? { "data-gsap": dataGsap } : {};
---

<Tag class={"card" + (hover ? " card--hover" : "")} {...attrs}>
  <slot />
</Tag>

<style>
  .card {
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: 28px;
  }
  .card--hover {
    transition: background var(--duration-base) ease, border-color var(--duration-base) ease, transform var(--duration-base) var(--ease-out);
  }
  .card--hover:hover {
    background: var(--color-bg-card-hover);
    border-color: var(--color-border-strong);
    transform: translateY(-3px);
  }
</style>
